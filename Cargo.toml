[package]
name = "error_handling"
version = "0.1.0"
authors = ["kernel <nbelsito02@gmail.com>"]
edition = "2018"

# smaller binary result
[profile.release]
panic = 'abort'

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
